// Save parameters
public static class Data
{
    // progress
    public static int score = 0;
    public static int lastScore = 0;
    public static int topScore = 0;
    public static int coins = 30500;

    // settings
    public static string music = "on";
    public static string sound = "on";

    // store
    public static string[] items = { "ACTUAL", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED",
        "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED", "CLOSED" };

    // location
    public static int locationID = 0;
    public static string[] locations = { "ACTUAL", "LOCKED", "LOCKED", "LOCKED", "LOCKED" };

    // game
    public static int gameStart = 0;
    public static int sessions = 0;
    public static float averagedistance = 0f;
}